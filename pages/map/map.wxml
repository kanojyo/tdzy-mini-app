<view class='view'>
  <map style="width: 100%;height: {{height}}px;"  class='map' longitude="{{longitude}}" latitude="{{latitude}}" markers="{{markers}}" covers="{{covers}}" show-location>
    
  </map>
</view>
